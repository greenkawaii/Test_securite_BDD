Ouvrir docker descktop 

Lancer docker_compose.yml => création du contenaire avec BBD vide

Dans le terminal : pip install psycopg2-binary flask
    psycopg2 = driver postgreSQL
    flask = gestion API (pour plus tard)

Lancer le script users.py pour creer les utilisateurs avec mdp

